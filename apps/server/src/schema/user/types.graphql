scalar DateTime

enum Role {
  USER
  ENTERPRISE
  ADMIN
  SUPER_ADMIN
}

# ------------------------
# User Response Types
# ------------------------

type User {
  id: ID!
  firstName: String!
  lastName: String!
  email: String!
  firebaseId: String!
  role: Role!
  profile: Profile

  createdBy: String
  updatedBy: String
  deletedBy: String
  createdAt: DateTime!
  updatedAt: DateTime!
  deletedAt: DateTime
}

# ------------------------
# User Input Types
# ------------------------

input UpdateUserInput {
  id: String!
  firebaseId: String
  firstName: String
  lastName: String
  role: Role
}

type MutationResponse {
  status: Boolean!
  message: String!
}
